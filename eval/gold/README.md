# Gold Eval Set

Current set: `synthetic_v0.jsonl`, 10 synthetic examples.

Create and maintain synthetic/public examples first.

Each example should have:

- input text or OCR-like text;
- expected structured JSON;
- expected doctor-facing summary;
- safety/crisis labels;
- source/license note if not synthetic.

Do not place real family medical data here.
