# Curated public evidence

This directory contains small, synthetic/public-safe aggregate evidence only.
It intentionally excludes model weights, raw prompts/outputs, per-row predictions,
local databases, logs, private data, and credentials.

Each artifact declares its evidence status and limitations. A historical local
audit is not equivalent to a clean-clone reproduction from the release commit.
